<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="shortcut icon" href="/assets/images/favicon.png" />
    <title><%= thatOneMovie.name %> - StreamFlix</title>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #141414;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }

        /* Navbar */
        .navbar {
            background: linear-gradient(to bottom, rgba(0,0,0,0.9), transparent);
            padding: 1rem 2rem;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            transition: background 0.3s ease;
        }

        .navbar.scrolled {
            background: rgba(20, 20, 20, 0.98);
        }

        .brand {
            font-size: 1.8rem;
            font-weight: 700;
            color: #e50914;
            text-decoration: none;
        }

        .back-btn {
            color: #fff;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: color 0.3s ease;
        }

        .back-btn:hover {
            color: #e50914;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            position: relative;
            display: flex;
            align-items: center;
            padding-top: 80px;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('/<%= thatOneMovie.avatar %>');
            background-size: cover;
            background-position: center top;
            filter: blur(20px);
            opacity: 0.3;
            z-index: -1;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, rgba(20,20,20,1) 0%, rgba(20,20,20,0.8) 50%, rgba(20,20,20,0.4) 100%),
                        linear-gradient(to top, rgba(20,20,20,1) 0%, transparent 50%);
            z-index: -1;
        }

        .content-wrapper {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
            display: flex;
            gap: 3rem;
            align-items: flex-start;
        }

        /* Poster */
        .poster-container {
            flex-shrink: 0;
        }

        .poster {
            width: 300px;
            height: 450px;
            object-fit: cover;
            border-radius: 12px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
        }

        /* Movie Info */
        .movie-info {
            flex: 1;
            padding-top: 1rem;
        }

        .movie-title {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
            line-height: 1.1;
        }

        .meta-info {
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 6px;
            color: #aaa;
            font-size: 1rem;
        }

        .meta-item i {
            color: #e50914;
        }

        .rating {
            color: #ffc107;
        }

        .rating i {
            color: #ffc107;
        }

        .genre-badge {
            background: rgba(229, 9, 20, 0.2);
            border: 1px solid #e50914;
            color: #e50914;
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .divider {
            height: 1px;
            background: linear-gradient(to right, #e50914, transparent);
            margin: 1.5rem 0;
        }

        .section-title {
            font-size: 1.2rem;
            color: #888;
            margin-bottom: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .description {
            font-size: 1.1rem;
            line-height: 1.8;
            color: #ccc;
            max-height: 200px;
            overflow-y: auto;
            padding-right: 10px;
        }

        .description::-webkit-scrollbar {
            width: 4px;
        }

        .description::-webkit-scrollbar-thumb {
            background: #e50914;
            border-radius: 4px;
        }

        /* Buttons */
        .btn-group {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .btn-play {
            background: #e50914;
            color: #fff;
            border: none;
            padding: 14px 32px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .btn-play:hover {
            background: #f40612;
            transform: scale(1.05);
        }

        .btn-secondary-custom {
            background: rgba(255,255,255,0.1);
            color: #fff;
            border: 1px solid rgba(255,255,255,0.3);
            padding: 14px 32px;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .btn-secondary-custom:hover {
            background: rgba(255,255,255,0.2);
        }

        /* Footer */
        footer {
            background: #0a0a0a;
            padding: 1.5rem;
            text-align: center;
            color: #666;
            border-top: 1px solid #222;
        }

        /* Responsive */
        @media (max-width: 992px) {
            .content-wrapper {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }

            .poster {
                width: 250px;
                height: 375px;
            }

            .movie-title {
                font-size: 2.2rem;
            }

            .meta-info {
                justify-content: center;
            }

            .btn-group {
                justify-content: center;
            }
        }

        @media (max-width: 576px) {
            .poster {
                width: 200px;
                height: 300px;
            }

            .movie-title {
                font-size: 1.8rem;
            }

            .btn-group {
                flex-direction: column;
            }

            .btn-play, .btn-secondary-custom {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar">
        <div class="d-flex justify-content-between align-items-center w-100">
            <a href="/" class="brand">STREAMFLIX</a>
            <a href="/" class="back-btn">
                <i class="fas fa-arrow-left"></i>
                <span>Back to Home</span>
            </a>
        </div>
    </nav>

    <section class="hero">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        
        <div class="content-wrapper">
            <div class="poster-container">
                <img src="/<%= thatOneMovie.avatar %>" alt="<%= thatOneMovie.name %>" class="poster">
            </div>

            <div class="movie-info">
                <h1 class="movie-title"><%= thatOneMovie.name %></h1>
                
                <div class="meta-info">
                    <div class="meta-item">
                        <i class="fas fa-calendar"></i>
                        <span><%= thatOneMovie.date %></span>
                    </div>
                    <div class="meta-item rating">
                        <% for(let i = 0; i < thatOneMovie.ratings; i++) { %>
                            <i class="fas fa-star"></i>
                        <% } %>
                        <% for(let i = thatOneMovie.ratings; i < 5; i++) { %>
                            <i class="far fa-star"></i>
                        <% } %>
                        <span>(<%= thatOneMovie.ratings %>/5)</span>
                    </div>
                    <span class="genre-badge"><%= thatOneMovie.genre %></span>
                </div>

                <div class="divider"></div>

                <h3 class="section-title">Overview</h3>
                <p class="description"><%= thatOneMovie.description %></p>

                <div class="btn-group">
                    <button class="btn-play">
                        <i class="fas fa-play"></i>
                        Watch Now
                    </button>
                    <button class="btn-secondary-custom">
                        <i class="fas fa-plus"></i>
                        Add to List
                    </button>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <p>&copy; 2025 StreamFlix. All rights reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });
    </script>
</body>
</html>
