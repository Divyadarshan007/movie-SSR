<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <title><%= thatOneMovie.name %> - Details</title>

    <style>
        /* Custom Dark Theme Styles */
        body {
            background-color: #141414 !important;
            color: white;
        }

        /* Smooth scrollbar for the description box */
        .description-box {
            max-height: 40vh; /* Limits height to 40% of viewport */
            overflow-y: auto; /* Adds scroll if text is too long */
            padding-right: 15px; /* Space for scrollbar */
            scrollbar-width: thin; /* Firefox */
            scrollbar-color: #e50914 #333; /* Firefox */
        }

        /* Webkit Scrollbar Styling (Chrome/Safari/Edge) */
        .description-box::-webkit-scrollbar {
            width: 8px;
        }
        .description-box::-webkit-scrollbar-track {
            background: #333;
            border-radius: 4px;
        }
        .description-box::-webkit-scrollbar-thumb {
            background-color: #e50914; /* Netflix Red */
            border-radius: 4px;
            border: 2px solid #333;
        }

        /* Image sizing on mobile */
        @media (max-width: 768px) {
            #detail-poster {
                max-width: 250px; /* Prevents poster from being huge on mobile */
                margin: 0 auto 20px auto; /* Centers image */
                display: block;
            }
            .text-content-wrapper {
                text-align: center; /* Center text on mobile */
            }
            .d-flex.gap-3 {
                justify-content: center; /* Center badges on mobile */
            }
        }
    </style>
</head>

<body class="bg-black text-white">

    <div class="container py-5">
        
        <button class="btn btn-outline-light mb-4" onclick="window.location.href='/'">
            <i class="fas fa-arrow-left"></i> Back to Home
        </button>

        <div class="row align-items-start g-4"> <div class="col-12 col-md-4 col-lg-3">
                <img id="detail-poster" src="/<%= thatOneMovie.avatar%>" class="img-fluid rounded shadow-lg" alt="Poster">
            </div>

            <div class="col-12 col-md-8 col-lg-9 text-content-wrapper">
                
                <h1 class="display-4 display-md-2 fw-bold" id="detail-title"><%= thatOneMovie.name%></h1>

                <div class="d-flex flex-wrap gap-3 my-3 text-secondary fs-5 align-items-center">
                    <span id="detail-year" class="fw-bold text-white"><%= thatOneMovie.date%></span>
                    
                    <span class="text-warning">
                        <i class="fas fa-star"></i> 
                        <span id="detail-rating"><%= thatOneMovie.ratings%></span>
                    </span>
                    
                    <span id="detail-genre" class="badge border border-secondary fw-normal text-uppercase spacing-1">
                        <%= thatOneMovie.genre%>
                    </span>
                </div>

                <hr class="border-secondary opacity-25">

                <div class="description-box mt-4">
                    <p class="lead text-light opacity-75" id="detail-desc" style="line-height: 1.8;">
                        <%= thatOneMovie.description%>
                    </p>
                </div>

                <div class="mt-4">
                    <button class="btn btn-danger btn-lg me-2"><i class="fas fa-play"></i> Watch</button>
                    <button class="btn btn-dark btn-lg border-secondary"><i class="fas fa-plus"></i> List</button>
                </div>

            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>